# Laravel Application Skeleton

This directory contains a Laravel 11 application skeleton configured for PHP 8.3.

## Getting Started

```bash
composer install
cp .env.example .env
php artisan key:generate
```

## Frontend

The Vite build pipeline ships with Tailwind CSS and Alpine.js presets.
